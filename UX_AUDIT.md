# UX/UI-аудит платформы «Репетитор — кабинет»

> Роль ревьюера: **новый репетитор (20–60 лет)**, впервые открыл кабинет и включил демо-режим.
> Проверено: онбординг, все 6 разделов, все всплывающие окна, светлая и тёмная темы, десктоп (1280 / 1440 / 1600), мобайл (375px).
> Формат: находка → почему это проблема → **альтернатива**. В конце — сводная таблица приоритетов.

Легенда приоритетов: 🔴 критично · 🟠 заметно · 🟡 мелочь/полировка.

---

## 0. Сначала о хорошем (чтобы было от чего отталкиваться)

- Живое превью отчёта родителю справа от конструктора — сильное, понятное решение.
- Онбординг + туториал из 8 шагов + контекстные подсказки `?` — забота о новичке видна.
- Мобильный календарь превращается в agenda-список по дням — правильный паттерн.
- Тумблеры, подсказки и статусные пилюли оформлены аккуратно и единообразно.
- Есть `prefers-reduced-motion` — уважение к пользователям с чувствительностью к анимации.

Дальше — что улучшить.

---

## 1. Accessibility (доступность)

### 1.1 🔴 Кликабельные элементы — это `div`, недоступные с клавиатуры
События календаря, строки списков, карточки учеников — это `div` с `onclick`, без `role`, `tabindex` и обработки Enter/Space. Их нет в дереве доступности (проверено: `read_page` не видит ни одного события календаря как интерактивный элемент; на всю страницу `tabindex="0"` стоит только у 8 подсказок-`?`).
**Почему:** пользователь с клавиатурой или скринридером физически не может открыть занятие или карточку.
**Альтернатива:** сделать их `<button>` (или добавить `role="button"` + `tabindex="0"` + обработчик Enter/Space). Это же добавит фокус-обводку «бесплатно».

### 1.2 🔴 Нет видимого фокуса на кнопках и навигации
В стилях `:focus-visible` встречается ровно один раз (у тумблера), и то через переменную `--ring`, **которая нигде не объявлена** — значит обводка либо не рисуется, либо непредсказуема. У кнопок меню, `.btn`, крестиков-закрытий фокус-состояния нет вообще (есть только `:hover` с `translateY`).
**Почему:** невозможно понять, где сейчас фокус при навигации Tab'ом.
**Альтернатива:** задать `--ring` (например `rgba(239,79,145,.45)`) и общее правило `:focus-visible{outline:3px solid var(--ring);outline-offset:2px}` для всех интерактивных элементов.

### 1.3 🟠 Модальные окна не помечены как диалоги
`role="dialog"`/`aria-modal="true"` стоит только у одного окна (`appDialog`). Остальные 8 (ученик, занятие, оплата, событие, группа, карточка, онбординг, туториал) — обычные `div`. Нет и ловушки фокуса (focus trap): Tab уводит фокус на страницу под модалкой.
**Альтернатива:** добавить `role="dialog" aria-modal="true" aria-labelledby=<id заголовка>` и замыкание фокуса внутри окна. Escape уже работает — это плюс.

### 1.4 🟠 Тосты и статус сохранения не озвучиваются
У `#toast` и индикатора «Сохранено» нет `aria-live` (проверено: `aria-live` в файле 0 штук).
**Почему:** незрячий пользователь не узнает, что «Отчёт сохранён» или «Не удалось сохранить».
**Альтернатива:** `aria-live="polite"` на тост, `aria-live="assertive"` на ошибки облака.

### 1.5 🟠 Контраст активного пункта меню ниже нормы
Активный пункт: розовый текст `#ef4f91` на розовой подложке `#fff1ec` — контраст ≈ **2.9:1** при норме AA 4.5:1. То же касается розовых пилюль-целей и `.notice`.
**Альтернатива:** для активного пункта затемнить текст (`#c9256b`) или усилить фон; проверить палитру в контраст-чекере.

### 1.6 🟡 Индикатор времени суток `◐` без подписи
Символ `◐` рядом с приветствием — чисто декоративный, без `aria-hidden` и без текстовой альтернативы (подробнее в 4.1).

---

## 2. Дизайн: где наляписто / сложно / можно упростить

### 2.1 🟠 Тотальный розовый — не хватает нейтральных пауз
Бренд-розовый/коралловый используется одновременно на: логотипе, активном меню, всех первичных кнопках, пилюлях целей, событиях календаря, подсказках, левой рамке групп, аватарах. Глазу не за что «зацепиться» — всё кричит одинаково.
**Альтернатива:** оставить розовый только для 1 первичного действия на экран и акцентов; вторичные элементы (пилюли целей, события) увести в нейтральный серый/бежевый, а цветом кодировать **смысл** (статус, долг), а не украшение.

### 2.2 🟠 Два одинаковых оранжевых блока подряд
И в карточке ученика, и в модалке занятия сверху стоят **два** мягко-оранжевых блока подряд (подсказка + «Предыдущее ДЗ / Последняя тема», либо «Следующее занятие» + «Цели/Заметки»). Одинаковый цвет и вес → сливаются в «стену».
**Альтернатива:** объединить в один блок с разделителем, либо один сделать акцентным (оранжевый), а второй — нейтральным (серый) с иконкой.

### 2.3 🟠 Инвертированная логика цвета у чипов-целей
В форме ученика невыбранные цели — **розовые** (выглядят как «включённые»), а выбранные — зелёные. Это противоречит ожиданию «бренд-цвет = активно».
**Альтернатива:** невыбранные — нейтральный контур (серый), выбранные — заливка бренд-цветом с галочкой. Тогда состояние читается мгновенно.

### 2.4 🟡 Кнопка «Заполнено» выглядит кликабельной, но пассивна
На дашборде у проведённого занятия стоит рамка-«кнопка» **Заполнено**, визуально неотличимая от активной кнопки «Заполнить информацию».
**Альтернатива:** сделать это статусной пилюлей с галочкой (зелёный `✓ Заполнено`), а не кнопкой.

### 2.5 🟡 «Обновить из истории» — неочевидная формулировка
Кнопка перезаполняет темы/ДЗ/проверочные из занятий, но новичок не догадается по названию (и есть отдельная подсказка `?`, что уже признак проблемы формулировки).
**Альтернатива:** «Собрать из проведённых занятий» / «Заполнить автоматически».

### 2.6 🟡 Оплаты потеряли сводку по деньгам
Раздел «Оплаты» показывает только долги/абонементы по ученикам. Итоговых чисел «Получено за месяц / Начислено / Всего долгов» на экране нет (в коде они есть в `renderPayments`, но используется `renderPaymentsSimple`, который прячет KPI-строку).
**Почему:** репетитору важна цифра «сколько я заработал». Сейчас её негде увидеть.
**Альтернатива:** вернуть компактную строку из 2–3 KPI (Получено за 30 дней · Ожидается · Долги) над таблицей.

---

## 3. Где добавить анимации

Сейчас есть базовые `pageIn`, `modalIn`, `tutorialPop`, ховер-`translateY`. Чего не хватает:

- 🟠 **Тумблеры и раскрытие полей.** При включении «Была проверочная работа» / «Было предыдущее ДЗ» блок появляется мгновенно (`display:none`→`block`), контент «прыгает». → плавный height/opacity-переход.
- 🟡 **Тост.** Уже есть transition, но появляется резко снизу — добавить лёгкий «bounce» и авто-progress до исчезновения.
- 🟡 **Прогресс-кольцо в отчёте.** `stroke-dashoffset` меняется скачком — анимировать заполнение (0 → N%) за ~0.6s. Это самый «вкусный» момент отчёта.
- 🟡 **Числа-метрики** в карточке ученика (посещаемость, средняя оценка) — count-up при открытии.
- 🟡 **Удаление строки** (платёж, слот расписания) — сейчас исчезает мгновенно; добавить fade+collapse, чтобы было понятно, что именно удалилось.
- 🟡 **Бейдж-счётчик** «Оплаты · 1» — лёгкий pulse при появлении нового долга.

> Всё — под `@media (prefers-reduced-motion)`, который уже подключён.

---

## 4. Цвета, акценты, иконки

### 4.1 🟠 Символ `◐` у приветствия выглядит как сломанная иконка
`<span class="greeting-visual evening">◐</span>` — геометрический полукруг (U+25D0), задуман как индикатор времени суток, но читается как глитч/недогруженный эмодзи. Особенно заметен в тёмной теме и на мобайле.
**Альтернатива:** нормальная иконка солнца/луны (SVG) с `aria-hidden="true"`, либо убрать совсем — приветствие «Добрый вечер» уже несёт эту информацию.

### 4.2 🟠 Разнородный набор иконок в навигации
Иконки меню смешаны: `⌂ ▦ ₽ ✎ ⚙` — это типографские символы разной толщины и стиля, а `👥` (Ученики) — **цветной эмодзи**. В ряду выглядит инородно (особенно в нижнем таб-баре на мобайле).
**Альтернатива:** единый монохромный SVG-сет (line-иконки одной толщины), либо единый эмодзи-сет — но не микс.

### 4.3 🟡 Статусы занятий не различаются цветом
В календаре «Запланировано», «Проведено», «Перенесено», «Отменено» — **все одинакового розового цвета**, различие только в мелком сером тексте. Отменённое занятие выглядит как активное.
**Альтернатива:** цветовой код статуса — серый/зачёркнутый для «Отменено», пунктир для «Перенесено», зелёная точка для «Проведено», обычный для «Запланировано». (`.custom-event` уже имеет синюю рамку — можно масштабировать подход.)

### 4.4 🟡 Иконка удаления (корзина) — низкий контраст и слишком заметна
Серый `🗑`-квадрат на каждой карточке ученика/группы (см. также 6.4).

---

## 5. Адаптивность

### 5.1 🔴 Календарь ломается на 1280px (частая ширина ноутбука)
На ширине ≤1280px сетка календаря переключается на **4 колонки**, но подписи дней недели (7 штук: Пн Вт Ср Чт / Пт Сб Вс) остаются в той же сетке и разъезжаются: заголовки образуют строку 4+3, которая **не совпадает** с колонками дней (день «3 авг» — четверг — оказывается в 4-й колонке под подписью «Чт», а остальные подписи не совпадают ни с чем). Проверено: `grid-template-columns: 225px×4` при 7 подписях.
**Почему:** 1280px — типичный MacBook/ноутбук; календарь становится нечитаемым.
**Альтернатива:** либо держать 7 колонок с горизонтальным скроллом до более узкого брейкпоинта, либо на 4-колоночной сетке убрать строку-«шапку» дней недели и писать день недели прямо в ячейке (как уже сделано для мобайла через `.mobile-weekday`).

### 5.2 🟠 На мобайле пустые карточки съедают весь экран
Дашборд в один столбец: карточка «Предстоящие сегодня» с пустым состоянием (эмодзи 🧘 по центру) занимает ~500px высоты ради одной строки «занятий нет». Важные «Прошедшие» и «Незаполненные» уезжают далеко вниз.
**Альтернатива:** на мобайле схлопывать пустые карточки в одну строку («Сегодня занятий нет») или показывать сначала непустые секции; убрать `min-height` у пустых состояний.

### 5.3 🟡 Две первичные кнопки в шапке расписания на мобайле
«+ Своё событие» и «+ Разовое занятие» стоят двумя блоками во всю ширину друг под другом и отжимают календарь вниз.
**Альтернатива:** одна первичная кнопка «+ Занятие», внутри выбор типа; либо две кнопки в ряд 50/50.

### 5.4 🟡 На мобайле нет плавающей кнопки быстрого действия
`.quick` (FAB) в стилях есть, но принудительно `display:none`. При этом основной сценарий на телефоне — быстро «Заполнить занятие». FAB здесь был бы уместнее, чем скролл наверх к кнопкам.

---

## 6. Баги и рискованные места (демо-режим)

### 6.1 🔴 Авто-фокус на деструктивной кнопке в диалоге подтверждения
`appDialog` после открытия делает `confirmBtn.focus()` **всегда**, включая опасные диалоги («Очистить все данные?», «Удалить ученика»). Красная кнопка «Очистить всё» получает фокус → случайный **Enter стирает все данные**.
**Альтернатива:** для `danger`-диалогов фокусировать «Отмена», а подтверждение требовать явным кликом (можно + удержание/двойное подтверждение для «Очистить всё»).

### 6.2 🟠 Дубликат занятия на дашборде
Одно и то же занятие «Максим Орлов, 20:31, Проведено» показывается **одновременно** в «Прошедшие сегодня» и в «Незаполненные». Пользователь думает, что это два разных дела.
**Альтернатива:** если занятие сегодня и не заполнено — показывать в одной секции; в «Незаполненные» выводить только записи прошлых дней.

### 6.3 🟠 Демо-данные генерируют нереалистичное время
Времена занятий в демо привязаны к `Date.now()`, поэтому в кабинете появляются уроки в **23:31 и 01:31**. Демо — это первое впечатление нового репетитора, и оно выглядит «сырым».
**Альтернатива:** фиксировать демо-слоты на человеческих временах (16:00–19:00), не привязываясь к текущему часу.

### 6.4 🟠 Кнопка удаления прямо на карточке — шум + риск
На каждой карточке ученика и группы в углу висит корзина, удаляющая ученика в один тап (пусть и с подтверждением). Для новичка это и визуальный шум, и риск.
**Альтернатива:** убрать удаление внутрь карточки/меню «⋯»; на сетке оставить только клик-открытие.

### 6.5 🟠 Первичное действие вне зоны видимости (скролл)
Модалка «Информация о занятии» — 1258px контента при видимой высоте ~790px (десктоп) и 1623px при 812px (мобайл). Футер с кнопкой **«Сохранить отчёт»** — `position: static`, то есть **не виден без прокрутки** через всю длинную форму. Шапка при этом `sticky`, а футер — нет.
**Почему:** прямо нарушает требование «важные действия — во вьюпорте». Легко заполнить форму и уйти, не сохранив.
**Альтернатива:** сделать `.modal-foot` липким (`position:sticky; bottom:0`) с фоном и тенью — «Сохранить» всегда на виду. На мобайле — открывать такие формы как full-screen sheet со sticky шапкой и футером.

### 6.6 🟡 Разный смысл в одной ячейке карточки ученика
Третья метрика на карточке — то «1 зан. · Абонемент», то «4 200 ₽ · Долг», то «0 ₽ · Долг». Одна позиция — разные сущности (остаток абонемента vs деньги).
**Альтернатива:** зафиксировать подпись («Баланс») и единый формат, либо явно разделять «Абонемент» и «Долг» как разные бейджи.

### 6.7 🟡 Мелкая типографика в мобильном календаре
Заголовки дней: «Четверг,6 Авг.» — нет пробела после запятой (и лишняя капитализация «Авг.»).
**Альтернатива:** «Четверг, 6 авг.».

---

## 7. Сводная таблица приоритетов

| # | Находка | Категория | Приоритет |
|---|---------|-----------|-----------|
| 6.1 | Авто-фокус на «Очистить всё» → Enter удаляет данные | Баг/безопасность | 🔴 |
| 5.1 | Разъезжается шапка дней недели на 1280px | Адаптивность | 🔴 |
| 1.1 | Кликабельные `div` недоступны с клавиатуры | A11y | 🔴 |
| 1.2 | Нет видимого фокуса + `--ring` не объявлена | A11y | 🔴 |
| 6.5 | «Сохранить отчёт» вне вьюпорта (футер не sticky) | Вьюпорт | 🔴 |
| 6.2 | Дубль занятия «Прошедшие» + «Незаполненные» | Баг | 🟠 |
| 6.3 | Демо-время 23:31/01:31 | Баг/демо | 🟠 |
| 6.4 | Удаление ученика прямо на карточке | Дизайн/риск | 🟠 |
| 2.1 | Переизбыток розового | Дизайн | 🟠 |
| 2.2 | Два оранжевых блока подряд | Дизайн | 🟠 |
| 2.3 | Инвертированный цвет чипов-целей | Дизайн | 🟠 |
| 2.6 | Нет сводки доходов в «Оплатах» | Дизайн | 🟠 |
| 4.1 | Символ `◐` как сломанная иконка | Цвет/иконки | 🟠 |
| 4.2 | Разнородные иконки меню (эмодзи + символы) | Иконки | 🟠 |
| 4.3 | Статусы занятий не кодируются цветом | Цвет | 🟠 |
| 5.2 | Пустые карточки съедают мобильный экран | Адаптивность | 🟠 |
| 1.3 | Модалки без `role="dialog"` и focus-trap | A11y | 🟠 |
| 1.4 | Тосты без `aria-live` | A11y | 🟠 |
| 1.5 | Контраст активного меню ≈2.9:1 | A11y | 🟠 |
| 3.* | Отсутствие микро-анимаций (раскрытие полей, кольцо) | Анимации | 🟡 |
| 2.4 / 2.5 / 6.6 / 6.7 / 5.3 / 5.4 / 4.4 | Полировка формулировок, метрик, иконок | Разное | 🟡 |

---

### Топ-5, с чего начать
1. **Убрать авто-фокус с деструктивной кнопки** (6.1) — минимальная правка, устраняет риск потери данных.
2. **Сделать футер модалок sticky** (6.5) — «Сохранить» всегда виден.
3. **Починить шапку календаря на 1280px** (5.1).
4. **Дать фокус-обводку и объявить `--ring`** (1.2) + превратить кликабельные `div` в кнопки (1.1).
5. **Заменить `◐` и унифицировать иконки** (4.1, 4.2) — дёшево, сразу поднимает «дороговизну» интерфейса.
